# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.env

# Weights: ignore stray .pt files except bundled checkpoints under weights/ (assignment submission)
*.pt
!weights/*.pt

runs/
# outputs/ kept for assignment examples (segmentation JPEGs)

# Datasets (large; do not push to GitHub — recreate with scripts/prepare_coco_val5k.py, train_segment_finetune.py, etc.)
datasets/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
